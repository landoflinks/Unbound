<Window x:Class="Unbound.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Unbound"
        mc:Ignorable="d"
        Title="Unbound - The Earth At Your Fingertips!" Height="450" Width="586.686">

    <Grid>

        <StackPanel Margin="0,0,0,-61.2">

            <!-- Search Bar -->
            <TextBlock Text="Search Location" Padding="5" FontWeight="Bold" />
            <TextBox x:Name="SearchTextBox" Margin="5,0,225,0" Background="white" Height="22" />


            <Grid Margin="0,25,0,1">
                <!-- Main map object -->
                <m:Map CredentialsProvider="AlUeSTQVv9GwuYMLV1Iyp3aiOgetXonrVPy8lFwNo5OBNqYQkKudpzbPm7FbURCg" Center="37.806029,-100.407007"
                       Mode="Road" x:Name="unboundMap" ZoomLevel="3.5" Margin="0,0,0,-311"/>

                <Grid Height="40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Map Orientation Button -->
                    <Button x:Name="btn_mapChange" Width="80" HorizontalAlignment="Left" Margin="5,0,0,0"
                    VerticalAlignment="Top" Click="btn_mapChange_Click" Grid.Column="0">
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect> Satellite
                    </Button>

                    <!-- Zoom In Button -->
                    <Button x:Name="btn_zoomIn" Width="20" HorizontalAlignment="Left" Margin="20,0,0,0"
                    VerticalAlignment="Top" Click="btn_zoomIn_Click" Grid.Column="5">
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect> +
                    </Button>

                    <!-- Zoom Out Button -->
                    <Button x:Name="btn_zoomOut" Width="20" HorizontalAlignment="Right" Margin="0,0,20,0"
                    VerticalAlignment="Top" Click="btn_zoomOut_Click" Grid.Column="5">
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect> -
                    </Button>
                </Grid>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
